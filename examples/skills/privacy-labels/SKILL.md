---
name: privacy-labels
description: 根据用户给定的实体类型定义检查标签名称与使用条件，不自行增加标签。
---
# 标签规范检查

## 输入
样本、已有标注以及本次任务的实体类型定义。

## 步骤
1. 确认允许的标签名称及每种标签的定义。
2. 检查标注是否使用了允许的标签，并引用原文和定义解释问题。
3. 定义不清或无法判断时记录不确定项，不把猜测当作正确答案。

## 输出
标签问题清单与依据。保留原文和原有标注记录，修改需要用户要求及对应工具权限。
