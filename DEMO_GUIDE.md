# 本轮推荐演示入口

先启动服务，首页展开“试试一个演示任务”，选择“隐私实体数据集”。读取合成文本与规则后，查看写入内容并选择“仅本次允许”；在“成果”中查看 dataset.json 和独立检查报告。

点击右下角小伴，问“emoji”检索边界规则，展开证据并读取原始记录。模拟模式会明确标注；配置真实模型后可提“为什么 emoji 会影响实体位置”等解释问题。选择某条小伴回答作为反馈对象，点赞、点踩或扔鸡蛋，展示软标签记录；不要将情绪反馈解释为质量真值。

`npm run desktop` 可打开 macOS 浮窗，演示跟随工作台、固定任务、收起与展开。主页面点击“运行详情”再切到“上下文”，查看模型容量拆分；切换模型，说明先验证目标窗口再提交，后台助手的模型保持固定。

规模数据使用 `reports/scale.json` 或 `npm run test:scale`，明确这是累计调用与加载，不是千级并发或千种真实外部服务。

下面保留已有机制演示步骤，运行面板现在需要先点“运行详情”展开。

---

# 课堂演示与讲解脚本

演示前运行 `npm start`，打开 http://127.0.0.1:4317。保留默认的“Demo · 标准”，无需模型密钥。每张场景卡创建独立示例工作区，可以重复演示；“新建任务”只切换界面，不会停止仍在后台运行的旧任务。

## 1. 完整任务链

点击“完整任务链”。先解释：模型提出下一步请求，Harness 决定如何加载能力、执行工具和等待授权。

观察右侧“工具”：先搜索目录，再加载 `code-debug` 和测试等工具，读取 Skill 的检查清单。切到“任务树”，后台分析在独立副本中运行，主 Agent 同时读取代码并运行测试。

聊天中出现修改授权时，查看具体路径和完整写入内容，点击“仅本次允许”。等待任务结束，在“产物”中查看测试日志：修改前数量相关测试失败，修改后原有四项测试通过。后台任务全部结束，本地执行资源为 0。

讲解重点：任务输出不是预先写好的成功状态。模拟器发出预定请求，但文件修改、测试退出码、授权阻塞和资源回收来自真实运行时。拒绝修改也可以走完流程，界面会保留拒绝证据。

## 2. 打断与后台资源

新建“打断与资源回收”。等待任务树中出现诊断进程的 PID。

先演示聊天补充：选择“立即改方向”，发送“先停止诊断，只给分析，不要修改文件”。观察旧轮次取消、诊断进程退出和新一轮只读分析。后台停止由这条演示指令中的“停止诊断”触发；一般改变方向默认保留已有后台分析。

再新建同一场景，在进程运行时点击输入框旁的停止按钮。应看到任务经历“正在停止”后变为“已停止”，资源列表归零。

专家追问时说明：

- 打断不是删除已经发生的副作用。已完成写入保留；本地未执行调用取消；旧模型即使迟到，也会因轮次版本不匹配而被丢弃。
- 停止按钮关闭整个任务范围，先禁止创建新子任务，再取消所有 Agent 和受管理进程，最后确认清理完成。
- 后端被 SIGKILL、操作系统崩溃、子进程主动脱离进程组等情况，不在此 Demo 的完整回收保证内。生产环境需要外部容器/进程监督器和租约。

## 3. 能力目录与千级容量

打开左侧“能力目录”，在工具和 Skill 两个页签搜索 `analytics` 或 `latency`。查看完整定义、来源标签和分页；在任务内点击加载。右侧“工具”中可以卸载已加载能力。

再启动“千次工具调用”。观察累计调用增长、上下文多次压缩以及事件时间线。此场景不需要修改授权。

讲解时区分三个概念：目录中存在千级能力、按需把少量定义加载进当前上下文、累计完成千次分发。它们都与“同时运行一千个工具”不同。活跃工具/Skill 有上限，模型不需要看到整个目录。

现场想复核数字可运行 `npm run test:scale`。报告不仅记录 1,005 次累计计算，还实际遍历了 1,200 个不同工具与 1,204 个 Skill。生成样例检验检索和路由容量；真实业务多样性、语义检索召回率需要其他数据集评估。

## 4. 上下文压缩与模型切换

启动“上下文与模型交接”。切到“上下文”，观察 token 估算、摘要和压缩次数；长日志原文在“产物”中单独保存。也可点击“手动压缩上下文”；历史不足时跳过是正常行为。

在输入框模型选择器切换到“Demo · 紧凑”。切换可在运行中请求，也可在完成后操作。若当前正执行工具，会先完整收齐这次交互，再交接。查看聊天中的切换说明、上下文面板的交接记录和产物中的 `model-handoff.json`。

讲解重点：新模型接收当前目标、用户约束、授权事实、近期结果、历史摘要、已加载能力和后台任务状态。后台 Agent 保持原模型。目标窗口装不下必要信息时拒绝切换，保留原上下文。

对“如何保证换模型后质量不降低”的回答：这里保证的是任务状态的连续性，尽量降低因信息丢失导致的退化。模型本身能力差异、摘要遗漏和长程推理质量不能靠交接机制完全补偿。当前没有自动能力评测或质量保证；后续可用同任务回归、工具参数成功率、约束遵守和结果复核来验证。

## 5. 安全与授权

启动“授权边界”。观察不可信 Skill 可以被读取，但请求工作区外文件会收到 `PATH_DENIED`，改写测试会收到 `POLICY_DENIED`。两项拒绝来自运行时规则，不会弹出可以绕过硬规则的授权框。

在另一个“完整任务链”中，可选择“本任务内允许此文件”，并在设置里撤销。持续授权绑定当前任务、工具和文件路径。撤销不仅使后续请求需要重新批准，也阻止尚在排队的已授权写入。

不要把这个例子讲成通用恶意 Skill 检测器：样本的不可信标签是预置的，没有实现自动语义安全判定。真正承担执行限制的是工具参数、路径、身份和授权检查。

## 适合制作 PPT 的叙述顺序

先用一次完整任务展示“模型之外为什么还需要 Harness”，再展开用户打断、后台任务归属、千级能力发现、上下文与模型交接。最后用安全反例和测试结果说明可信度。

架构图可直接参考实现说明中的 Mermaid；规模数字引用 `reports/scale.json`；每个功能用“操作—可见状态—保证的边界”组织讲解。原设计中的沙箱、租约、自动安全审核、质量评估和完整宠物系统应标为后续方向，不能写成已完成能力。
